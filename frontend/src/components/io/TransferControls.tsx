// Stub (S7). S12 replaces this with the Export and Import toolbar buttons.
import type { JSX } from "react";

export function TransferControls(): JSX.Element {
  return <div className="transfer-controls" />;
}
