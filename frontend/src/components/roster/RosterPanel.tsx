// Stub (S7). S10 replaces this with the roster drawer, shown while `state.rosterOpen` is true.
import type { JSX } from "react";

export function RosterPanel(): JSX.Element | null {
  return null;
}
