// Shared layout constants (plan contract C6). The task panel and the chart both draw a
// HEADER_HEIGHT sticky header and then one ROW_HEIGHT row per task, so their rows line up.
export const ROW_HEIGHT = 36;
export const HEADER_HEIGHT = 56;
export const TASK_PANEL_WIDTH = 420;
export const SIDEBAR_WIDTH = 220;
